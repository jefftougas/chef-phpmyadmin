---
driver:
  name: vagrant
  require_chef_omnibus: latest

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[phpmyadmin::default]
    attributes:
